variable "region" {# i can set up the variables and then i can use them later and if i need to change something i can do it from here

  description = "This is the varaible for the specific reghon in the aws cloud we can use it directly"
  default       = "us-west-2"   # so this is my default region 
}
